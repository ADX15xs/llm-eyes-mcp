# 2026-08-05

## ds-vision-skill 借鉴复评（YAGNI）
- 用户拍板：端口探测自动发现、跨 provider 降级链、PDF/文档通道三项**不借**。
  前两者不助核心；PDF 单工具质量不够（用户另有需求分析项目：docx/xlsx 解压拆图 +
  图文多模态推理，纯文本转换丢内嵌图）。
- 核对代码：`internal/cache/keys.go` + tools 调用点确认 L2 缓存 key 已含 prompt
  （describe→question / extract→hint / measure→labels），无脏缓存风险，比上游更严谨。
- 唯一建议借：README 隐私声明（零代码，契合敏感需求文档场景）；provenance 可选；confidence 不借。
- README 已加精简版 `## Inspiration`（两句话），灵感来源为 ds-vision-skill。
- **终裁（10:24）**：隐私声明、provenance 也**不借**。隐私声明——默认模板已是 GLM、配 key 者
  有基本安全意识、开源可审计，为钻牛角尖者加声明冗余；provenance——MCP 回 LLM 是 JSON、展示由
  LLM 决定，要展示需配套提示词冗余，将来审计可加读 log 的 tool。结论：上游已彻底挖净，无剩余可借项。

## README 文案调整（11:13）
- 顺了「设计初衷」「灵感来源」两段中文表达；删掉上游「跨 provider 降级」表述（本项目未实现，误导）。
- 去掉「真正的 MCP stdio 服务器」里的「真正的」。
- 保留「确定性的测量能力」不改（用户：虽不严谨但有传播性）。
- 移除「运行」段里的「stdout 是保留通道」开发者告示——已在 docs/MCP_DEV_GUIDE.md §1.2
  与 AGENTS.md 覆盖，README 此处突兀。
